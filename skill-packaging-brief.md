# 将 SOP 封装为可安装 Agent Skill 的任务说明

## 一、任务目标

当前工作区已经完成了一整套“付费级 AI 教程写作 SOP”体系。

下一阶段的任务不是继续补文档，而是：

**把这套 SOP 封装成一个可复刻、可安装、可跨 Agent 使用的 Skill / Agent 包。**

目标是尽量支持下面这些环境：

- Codex
- Claude Code
- Cursor
- 如有余力，可兼容更多 Agent 环境

理想结果是：

**用户只需要执行尽量少的命令，最好一行安装命令，就能把这套 SOP 安装到新的 Agent 环境里使用。**

---

## 二、这个 Skill 最终应该解决什么问题

目前这套 SOP 虽然已经很完整，但它的使用成本 still 偏高。

当前问题是：

- 需要手动复制多份 `.md`
- 需要人自己知道先看哪份文档
- 需要新 Agent 手动理解文档之间的关系
- 跨环境复用还不够“开箱即用”

所以新的 Skill / Agent 包要解决的是：

1. 让 SOP 能被快速安装
2. 让 SOP 能被不同 Agent 自动识别或最小成本接入
3. 让用户不需要手动讲太多背景
4. 让 Agent 一进入环境，就知道该如何按这套 SOP 工作

---

## 三、当前这套 SOP 已经具备的核心能力

封装时要完整保留下面这些能力：

### 1. 双模式分流

- 改写已有原稿
- 从0撰写新稿

### 2. 模式不明确时先问用户

如果用户没有明确说是“改写”还是“从0撰写”，Agent 必须先问，不允许直接猜。

### 3. 三类文章类型识别

- 推荐型
- 教程型
- 方法论型

### 4. 官方优先的调研逻辑

优先级：

1. 官方文档
2. 官方仓库 / 官方博客 / 作者资料
3. 高质量第三方教程
4. 社区讨论辅助

### 5. 从0撰写模式下的默认调研模板

进入从0撰写模式时，Agent 必须先提示用户：

- 可以使用默认调研模板
- 也可以提供自己的调研模板

如果用户回复：

```text
没问题，开始工作吧
```

则默认使用：

- `从0撰写模式下的标准调研输出模板.md`

### 6. 付费级成稿要求

成稿必须尽量补齐：

- 核验日期
- 误区纠偏
- 踩坑提醒
- 适用边界
- 资料来源清单

---

## 四、封装目标不是“只打包文件”，而是“形成可用 Skill”

新 Agent 不应该只是把几个 `.md` 文件搬过去。

更理想的封装方向应该是：

- 有明确入口
- 有明确触发说明
- 有清晰的目录结构
- 有跨 Agent 的兼容说明
- 有最短安装方式
- 有最短使用示例

也就是说，它更像一个：

- 可安装 Skill
- 可复制的工作流包
- 或最小 Agent 插件化文档系统

---

## 五、建议交付物

新的 Agent 在完成这个任务时，建议至少交付下面这些内容：

### 1. Skill / 包的目录结构方案

需要说明：

- 主入口文件放哪里
- 哪些 `.md` 是核心文档
- 哪些是可选文档
- 不同 Agent 的适配目录怎么放

### 2. 安装方案

最好至少覆盖：

- Claude Code
- Codex
- Cursor

优先级建议：

1. 一行命令安装
2. CLI 安装
3. 手动复制安装

### 3. 使用说明

至少要说明：

- 安装后如何启动
- 用户该怎么发第一条指令
- Agent 如何识别改写 / 从0撰写模式

### 4. 最小示例

至少给 2 个：

- 一个“改写已有原稿”的示例
- 一个“从0撰写新稿”的示例

### 5. 兼容性说明

写清楚：

- 哪些 Agent 目前完整支持
- 哪些 Agent 只做到基础兼容
- 哪些特性需要人工配合

---

## 六、实现优先级建议

建议新的 Agent 按下面顺序推进，而不是一开始就追求“大而全”。

### 第一优先级

先让这套 SOP 能在 **Claude Code** 下被最小成本安装和稳定使用。

因为当前文档体系本身就是按 Claude Code / Agent Skills 逻辑抽象出来的。

### 第二优先级

再把目录结构适配到：

- Codex
- Cursor

### 第三优先级

最后才去考虑：

- 真正意义上的统一 CLI
- 一键安装脚本
- 多 Agent 自动分发

---

## 七、当前工作区里建议新 Agent 先读哪些文件

新的 Agent 接手这个任务时，建议优先阅读：

1. `README.MD`
2. `付费AI教程改写总控入口.md`
3. `付费AI教程改写标准流程-SOP.md`
4. `付费教程成稿模板-推荐型教程型方法论型.md`
5. `从0撰写模式下的标准调研输出模板.md`
6. `context-handoff.md`
7. `Progress.md`

如果还想看一个真实成品，再补读：

8. `七个实用 AI Skills 仓库推荐.md`

---

## 八、判断这个封装任务有没有做好，可以看这几个问题

新的 Agent 完成封装后，应该尽量让下面这些问题的答案变成“是”：

1. 一个新用户能不能少量命令安装这套 SOP？
2. 安装后，Agent 能不能自动进入这套工作逻辑？
3. 用户还需不需要自己记很多文档名？
4. 改写模式和从0撰写模式能不能都被保留？
5. 默认调研模板和自定义调研模板切换逻辑还在不在？
6. 推荐型 / 教程型 / 方法论型三种成稿骨架有没有被完整保留？

如果这些还做不到，就说明封装还没真正完成。

---

## 九、推荐新的 Agent 在新窗口里的起手动作

最推荐的第一步不是直接写代码，而是：

1. 先读核心文档
2. 再画出封装结构方案
3. 再决定是先做 Claude Code 版，还是同时兼容多个 Agent

也就是说，先做架构设计，再动手封装。

---

## 十、一句话总结这个任务

**不是继续写 SOP，而是把已经写好的 SOP 产品化、技能化、安装化。**
