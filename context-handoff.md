# 跨窗口记忆：当前对话核心要点

> 建立日期：2026-05-30  
> 用途：把当前对话中已经确定的工作规则、文档体系、执行方式整理成跨窗口可复用记忆

---

## 一、当前工作区已经形成的核心体系

当前目录不是单篇文章目录，而是一套完整的 **付费级 AI 教程写作系统**。

它覆盖两种模式：

1. `改写已有原稿`
2. `从0撰写新稿`

它覆盖三类文章：

1. `推荐型`
2. `教程型`
3. `方法论型`

目标不是普通润色，而是产出：

- 信息更准
- 结构更强
- 判断更清楚
- 更像中文顶级 AI 教程作者写的付费稿

---

## 二、当前根目录下各文档的职责

### 1. `付费AI教程改写总控入口.md`

这是总调度入口。  
以后新开窗口，优先引用这份文档。

作用：

- 先判断是“改写已有原稿”还是“从0撰写新稿”
- 再判断文章类型
- 再调度 SOP、模板、调研模板

### 2. `付费AI教程改写标准流程-SOP.md`

这是标准流程文档。

作用：

- 规定资料优先级
- 规定风格参考逻辑
- 规定任务卡、核验、误区、边界等标准

### 3. `付费教程成稿模板-推荐型教程型方法论型.md`

这是正文骨架库。

作用：

- 推荐型成稿模板
- 教程型成稿模板
- 方法论型成稿模板

### 4. `从0撰写模式下的标准调研输出模板.md`

这是从0写作前的调研整理模板。

作用：

- 先整理资料来源
- 先整理事实核验
- 先整理关键结论和结构建议
- 再进入正式正文

### 5. `新目录或新对话如何快速使用SOP与模板.md`

这是复用说明文档。

作用：

- 告诉以后在新目录、新窗口里怎么最快调用这套系统

### 6. `README.MD`

这是给人类用户看的总说明。

作用：

- 先讲这套体系的价值
- 再给快速使用 Prompt
- 再讲完整功能特性

---

## 三、以后工作时必须遵守的核心规则

### 1. 不允许只做润色

如果只是让原文更顺，不补结构、判断、边界，不算完成任务。

### 2. 先判断模式，再动手

每次任务先做二选一：

- `改写已有原稿`
- `从0撰写新稿`

如果用户没有明确说清楚，而且当前信息不足以安全判断，必须先问：

```text
你这次是要我基于已有原稿改写，还是根据主题从 0 撰写？
```

### 3. 再判断文章类型

只做三选一：

- 推荐型
- 教程型
- 方法论型

### 4. 先查官方资料，再参考第三方资料

优先级固定：

1. 官方文档
2. 官方仓库 / 官方博客 / 作者资料
3. 高质量第三方教程
4. 社区讨论辅助

### 5. 成稿必须补齐付费层内容

至少补：

- 核验日期
- 误区纠偏
- 踩坑提醒
- 适用边界
- 资料来源清单

### 6. 从0教程型任务必须先确认受众层级

如果任务同时满足：

- 属于“从0撰写新稿”
- 文章类型属于“教程型”

并且用户没有明确说清读者层级，就必须先补问：

1. 完全零基础
2. 用过一点但仍属小白
3. 已入门，想进阶到资深

这条规则已经写进：

- `AGENTS.md`
- `Skills/paid-ai-writing-sop/SKILL.md`
- `Skills/paid-ai-writing-sop/references/`
- `sop-docs/`

---

## 四、从0撰写模式下的特别规则

### 1. 不能直接开写

从0撰写不是凭印象展开，而是：

1. 先调研
2. 再整理
3. 再建结构
4. 最后写正文

### 2. 默认优先用网页搜索工具做调研

如果环境可用，默认优先用 `firecrawl` 或同级工具。

推荐顺序：

1. `firecrawl search`
2. `firecrawl scrape`
3. `firecrawl map`
4. `firecrawl crawl`

### 3. 正式调研前必须先发标准提示

只要判断出任务属于“从0撰写模式”，或者用户已经明确选择了这个模式，就必须先对用户发：

```text
我会按照《从0撰写模式下的标准调研输出模板.md》来完成调研。

如果您有自己的标准调研输出模板，可以在下面的对话中提供给我，我会按照你的调研模板来。

如果没有的话，你可以发送指令“没问题，开始工作吧”，在接下来的从0撰写文章的工作中，我将使用《从0撰写模式下的标准调研输出模板.md》作为默认的调研输出模板。
```

### 4. 只有两种情况，才进入正式调研

1. 用户提供了自己的调研模板
2. 用户明确回复了“没问题，开始工作吧”或等效确认

### 5. 如果是教程型，还要继续做受众分层

对“从0撰写 + 教程型”任务，进入正式调研前，现在默认需要同时满足：

1. 用户已经确认默认调研模板，或提供自定义模板
2. 教程受众层级已经明确，或可安全归类为 `1 / 2 / 3`

---

## 五、默认参考写作风格

当前主题如果属于：

- AI Skills
- Agent
- Prompt
- Claude Code / Codex / Cursor / OpenCode
- Make / n8n / 自动化工作流
- AI 工具推荐

默认参考逻辑：

- 结构推进：偏歸藏
- 解释拆解：偏宝玉
- 标题与导语抓力：借数字生命卡兹克
- 实操与工作流表达：参考翔宇工作流

注意：

- 学结构
- 学推进方式
- 学信息组织
- 不机械模仿口头禅

---

## 六、以后最常用的标准指令

### 1. 改写已有原稿

```md
请严格按照《付费AI教程改写总控入口.md》执行，改写 `xxx.md`。
```

### 2. 从0撰写新稿

```md
请严格按照《付费AI教程改写总控入口.md》执行。这次属于从0撰写新稿，请先按《从0撰写模式下的标准调研输出模板.md》完成调研，再正式写成付费级正文。
```

### 3. 先只看结构

```md
请严格按照《付费AI教程改写总控入口.md》执行，但先只输出模式判断、任务卡、文章类型判断、风格提取表和新的文章结构，暂时不要写全文。
```

### 4. 模式不明确时

```md
请严格按照《付费AI教程改写总控入口.md》执行，先判断这次任务应该走“改写已有原稿”还是“从0撰写新稿”，如果信息不足，请先向我确认模式，再继续后续工作。
```

---

## 七、当前工作区里已经产出的内容成果

当前已经完成的主要产物包括：

- `七个实用 AI Skills 仓库推荐.md`
  已重构为更接近付费教程标准的版本

- `付费AI教程改写标准流程-SOP.md`
  已扩成支持改写 + 从0撰写双模式

- `付费AI教程改写总控入口.md`
  已扩成双模式总控入口

- `付费教程成稿模板-推荐型教程型方法论型.md`
  已整理三类成稿骨架

- `从0撰写模式下的标准调研输出模板.md`
  已整理从0写作前的标准调研稿模板

- `README.MD`
  已整理成给人类看的总说明

- `Skills/paid-ai-writing-sop/references/tutorial-audience-and-visuals.md`
  已整理教程型任务的受众分层、可视化表达、案例组织和中文资料补充规则

---

## 八、以后跨窗口最短使用方式

如果以后新开窗口，只要当前根目录还在，最短做法就是：

1. 先引用 `付费AI教程改写总控入口.md`
2. 让 Agent 判断模式
3. 再按模式进入改写或从0撰写

一句话版本：

**以后任何新窗口，优先让 Agent 按《付费AI教程改写总控入口.md》执行。**

---

## 九、当前对话最后新增的要求

为了降低用户指令复杂度，已经明确规定：

当 Agent 判断出用户要“从0撰写文章”，或者用户已经选择了“从0撰写模式”时：

- 必须先提示默认使用 `从0撰写模式下的标准调研输出模板.md`
- 允许用户提供自己的调研模板替换
- 如果用户没有自己的模板，只要回复：

```text
没问题，开始工作吧
```

后续就使用默认调研模板继续工作

这条规则已经写进总控入口、调研模板和快速使用指南中。

### 2. 本轮又新增的教程型补强规则

为了避免“给小白教程却写成术语堆积稿”，当前又新增并已固化下面这些规则：

- 从0教程型任务必须先确认受众层级 `1 / 2 / 3`
- 面向 `1` 或 `2` 时，正文优先使用“完整案例 → 拆术语 → 第二个完整案例”
- 教程型内容在合适位置优先补 Mermaid、目录树、表格、伪输出示意
- 第三方资料不只看英文，也要尽量补知乎、掘金、阿里云开发者社区、GitHub 等中文高质量资料

这些规则已经同步写入：

- `Skills/paid-ai-writing-sop/`
- `sop-docs/`
- 当前根目录的记忆层文档

---

## 十、本轮对话新增的关键封装进展

### 1. 仓库已经收敛为标准 Skills 仓库结构

当前不再使用临时的 `skill-pack/` 双层结构，已经收敛为：

```text
Agent/
├── Skills/
│   └── paid-ai-writing-sop/
├── docs/
├── AGENTS.md
├── install.ps1
├── install.sh
├── README.MD
└── verify-install/
```

关键结论：

- 以后所有自定义 skills 统一放在 `Skills/`
- 当前主 skill 是 `Skills/paid-ai-writing-sop/`
- 根目录只放公共脚本、公共文档和兜底入口

### 2. `paid-ai-writing-sop` 已完成可安装 Skill 封装

当前主 skill 已具备：

- `SKILL.md`
- `references/`
- `assets/`

并且保留以下行为契约：

- 模式不明确时必须先问用户
- 从 0 模式必须先发送默认调研模板确认话术
- 用户未确认前不得进入正式调研
- 三类文章类型必须显式判断
- 成稿必须补齐核验日期、误区纠偏、踩坑提醒、适用边界、资料来源清单

### 3. 根目录安装脚本已经就位

当前根目录有：

- `install.ps1`
- `install.sh`

支持命令：

- `pwsh ./install.ps1 -Target all|codex|claude|cursor -Scope user|project`
- `bash ./install.sh --target all|codex|claude|cursor --scope user|project`

脚本行为：

- 自动创建目录
- 已存在时先备份再覆盖
- 安装后输出实际落点和验证提示

### 4. 已完成的安装验证

本轮对话已经做过两类验证：

#### 项目级烟测

保留目录：

- `verify-install/`

其中已生成：

- `verify-install/.agents/skills/paid-ai-writing-sop/`
- `verify-install/.claude/skills/paid-ai-writing-sop/`
- `verify-install/.cursor/skills/paid-ai-writing-sop/`

这个目录是保留的，不要删除，后续通过 `.gitignore` 忽略即可。

#### Claude Code 用户级安装

已经实际安装到：

- `C:\Users\LXW\.claude\skills\paid-ai-writing-sop`

理论上重新打开 Claude Code 后，可通过：

```text
/paid-ai-writing-sop
```

来验证是否已被发现。

### 5. README 已改造成安装优先

当前 `README.MD` 顶部已经补齐：

- 首次使用的最短 3 步流程
- `git clone` + `cd` 前提
- Codex / Claude Code / Cursor 的完整安装命令
- 其它兼容 Agent 的手动复制方式
- 非标准 Agent 的兜底读取方式

### 6. 允许在单个 skill 根目录增加给人类看的 `README.MD`

已经明确：

- 可以在 `Skills/paid-ai-writing-sop/` 目录里增加给人类看的 `README.MD`
- 一般不会影响 Agent 读取 `SKILL.md`
- 最好保持分工：
  - `SKILL.md` 写给 Agent
  - `README.MD` 写给人类
